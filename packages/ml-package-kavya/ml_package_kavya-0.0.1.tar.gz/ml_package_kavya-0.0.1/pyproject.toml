[tool.black]
line-length = 90
[tool.isort]
profile = "black"
float_to_top = true

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-package-kavya"
version = "0.0.1"
authors = [
    {name = "Kavya", email = "kavya.konakati@tigeranalytics.com"},
]
description = "ml_package installs the code required for predicting House price."
readme = "README.md"
requires-python = ">=3.11"