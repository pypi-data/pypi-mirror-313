{{cls}}(
{% for k, v in vars(val).items() %}
{% if attribute_overrides.get(_append(attribute_path, k)) %}
{{_spaces_for(indent+2)}}{{k}}={{attribute_overrides.get(_append(attribute_path, k))}},
{% else %}
{{_spaces_for(indent+2)}}{{k}}={{_render_value(v, _append(attribute_path, k), indent+2, classname_map, attribute_overrides, template_dir=template_dir)}},
{% endif %}
{% endfor %}
{{_spaces_for(indent)}})
