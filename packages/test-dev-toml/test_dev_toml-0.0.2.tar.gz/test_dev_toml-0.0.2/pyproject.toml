[build-system]
requires = ["setuptools >=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "test-dev-toml"
version = "0.0.2"
# additional packages to install
dependencies = [
    # "dep1",
    # "dep2",
]
requires-python = ">=3.9"
authors = [{ name = "J. Arrizza", email = "cppgent0@gmail.com" }]
maintainers = [
    { name = "J. Arrizza", email = "cppgent0@gmail.com" },
]
description = "test project for toml dev"
readme = {file = "README.md", content-type = "text/markdown"}
license = { file = "LICENSE.txt" }
keywords = ['setuptools', 'toml", "generation', 'python']

classifiers = [
    # Choose either "3 - Alpha", "4 - Beta" or "5 - Production/Stable"
    'Development Status :: 1 - Planning',
    'Environment :: Console',
    'Intended Audience :: Developers',
    'Topic :: Utilities',
    'Programming Language :: Python :: 3.9',
    'License :: OSI Approved :: MIT License',
]

[project.urls]
"Website" = "https://arrizza.com/test-me.html"
"Source" = 'https://bitbucket.org/arrizza-public/test-dev-toml/src/master'
"Download" = 'https://bitbucket.org/arrizza-public/test-dev-toml/get/master.zip'
# "Bug Reports" = "TODO https://github.com/pypa/sampleproject/issues"
# "Funding" = "TODO https://donate.pypi.org"
# "Say Thanks!" = "TODO http://saythanks.io/to/example"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where=["test_dev_toml"]
# exclude_package_data={'./test_dev_toml/lib': ['.gitignore']},
exclude = [".gitignore"]

[project.optional-dependencies]
# None
[project.scripts]
# None e.g. spam-cli = "spam:main_cli"


#setup(
# DONE   description='python module to generate Makefiles for arduino, GTest, /CC++, etc.',
# DONE   keywords=['makefile', 'generation', 'python'],
# TODO   install_requires=[ ],
# DONE   classifiers=[
# DONE       # Choose either "3 - Alpha", "4 - Beta" or "5 - Production/Stable"
# DONE       'Development Status :: 2 - Pre-Alpha',
# DONE       'Environment :: Console',
# DONE       'Intended Audience :: Developers',
# DONE       'Topic :: Utilities',
# DONE       'Programming Language :: Python :: 3.9',
# DONE       'License :: OSI Approved :: MIT License',
# DONE   ],
# DONE   name='test-dev-toml',
# DONE  packages=find_packages(include='./test_dev_toml*', ),
# TODO   include_package_data=True,
# TODO   exclude_package_data={'./test_dev_toml/lib': ['.gitignore']},
# DONE   version='0.0.1',
# DONE   license='MIT',
# DONE long_description=(Path(__file__).parent / 'README.md').read_text(),
# DONE   long_description_content_type='text/markdown',
# DONE   author='J. Arrizza',
# DONE   author_email='cppgent0@gmail.com',
# DONE  download_url='https://bitbucket.org/arrizza-public/test-dev-toml/get/master.zip',
#)

