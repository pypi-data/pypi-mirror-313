{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Remove unused imports",
            "type": "shell",
            "command": "autoflake --in-place --remove-unused-variables --remove-all-unused-imports --ignore-init-module-imports -r .",
            "problemMatcher": []
        }
    ]
}