[build-system]
requires = ["setuptools>=45", "wheel","colorama"]
build-backend = "setuptools.build_meta"

[project]
name = "dirview"
version = "0.1.5"
description = "A simple directory tree display tool"
readme = "README.md"
authors = [{name = "陈定钢", email = "945036663@qq.com"}]
requires-python = ">=3.6"

[project.scripts]
dirview = "dirview.main:main"

[tool.setuptools]
package-dir = {"" = "src"}
