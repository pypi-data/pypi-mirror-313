const config = require('./config.js')

const {main_00, main_01, sum} = require(config.root_03 + '/helper.js')
// ----------------------------------------------------------------------------------------------------

// ----------------------------------------------------------------------------------------------------
const getCookies = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getHeaders = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getParams = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getData = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
const getResponseBody = (o, ...args) => {
    return o
}
// ----------------------------------------------------------------------------------------------------
main_01(require.main === module)