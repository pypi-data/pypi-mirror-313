[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lsequbo"
version = "0.0.1"
authors = [
  { name="Orkun Şensebat", email="git@senseb.at" },
]
description = "QUBO problems for linear systems of equations (LSEs)"
readme = "README.md"
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]