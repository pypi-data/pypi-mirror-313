[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "boostmypy"
version = "0.1.1"
description = "Coming soon lib"
authors = [
    { name="Andrew Romanov", email="romanoffnv@gmail.com" }
]
license = { text = "MIT" }
dependencies = [
    # Add any dependencies here, e.g., "numpy", "pandas"
]
readme = "README.md" 