SCHEMA >
    `token` String,
    `workspace` String,
    `request_origin` String,
    `last_request_time` SimpleAggregateFunction(max, DateTime)

ENGINE AggregatingMergeTree
ENGINE_SORTING_KEY token, workspace, request_origin
