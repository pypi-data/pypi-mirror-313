TOKEN "workspaces_all (Data Source append)" APPEND


SCHEMA >
    `id` String,
    `name` String,
    `database` String,
    `database_server` String,
    `plan` String,
    `deleted` UInt8,
    `created_at` DateTime,
    `origin` String

ENGINE "ReplacingMergeTree"
ENGINE_PARTITION_KEY "tuple()"
ENGINE_SORTING_KEY "id"
