TOKEN "user_accounts_all (Data Source append)" APPEND

SCHEMA >
    `id` String,
    `email` String,
    `feature_flags` String,
    `deleted` UInt8,
    `created_at` DateTime

ENGINE ReplacingMergeTree
ENGINE_PARTITION_KEY tuple()
ENGINE_SORTING_KEY id

