[build-system]
requires = [
    "setuptools>=42",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "tmrvirus"
description = "Free Fire Module Includes Likes, Visit, Info,..."
version = "0.0.2"
requires-python = ">=3.7"
dependencies = [
                "requests",
                "Flask",
                "apscheduler",
                "protobuf",
                "pycryptodome",
                "websockets",
                "pyjwt",
                "tzlocal",
                "pytz"
                ]