[tool.poetry]
name = "platformspec"
version = "0.1.0"
description = "The Platform Specification Python SDK"
authors = ["Josh West"]
license = "Apache-2.0"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
pydantic = "^2.10.3"

[tool.poetry.group.dev.dependencies]
datamodel-code-generator = "^0.26.3"
pyyaml = "^6.0.2"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
