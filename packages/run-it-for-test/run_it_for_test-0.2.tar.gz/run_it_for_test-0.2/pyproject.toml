[tool.poetry]
name = "run-it-for-test"
version = "0.2"
description = "A decorator for marking and running example functions"
authors = ["sshling <rml0325@qq.com>"]
readme = "README.md"
license = "MIT"
packages = [{include = "run_it"}]

[tool.poetry.dependencies]
python = "^3.9"
typing-extensions = "^4.9.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
