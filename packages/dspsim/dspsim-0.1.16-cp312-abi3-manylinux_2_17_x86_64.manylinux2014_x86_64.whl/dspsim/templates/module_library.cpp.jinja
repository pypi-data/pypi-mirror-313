#include <dspsim/dspsim.h>
#include <dspsim/bindings.h>
{%for model in models%}
#include "{{model}}.h"
{%-endfor%}


NB_MODULE({{libname}}, m)
{
    dspsim::bind_module_context(m);
    
    {% for model in models %}
    {{model}}::bind_model(m, "{{model}}");
    {%-endfor%}
}
