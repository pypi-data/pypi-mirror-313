[tool.poetry]
name = "gd-st-custom-datatable"
version = "0.1.0"
description = ""
authors = ["Gnomon Digital <contact@gnomondigital.com>"]
readme = "README.md"
packages = [{include = "custom_datatable"}]
include = [
  { path = "custom_datatable/frontend/build/**/*", format = ["sdist", "wheel"] }
]

[tool.poetry.dependencies]
python = "^3.12"
streamlit = "^1.40.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
