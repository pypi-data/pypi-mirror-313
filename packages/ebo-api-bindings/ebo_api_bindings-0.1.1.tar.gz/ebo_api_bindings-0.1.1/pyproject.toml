[project]
name = "ebo-api-bindings"
version = "0.1.1"
description = "Simple KW bindings"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.32.3",
    "tqdm>=4.67.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
