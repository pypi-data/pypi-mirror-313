[package]
name = "signal_rs"
description = "A signal analysis libary written in Rust"
version = "0.1.0"
edition = "2021"

[dependencies]
pyo3 = { version = "0.18", features = ["extension-module"] }

[lib]
name = "signal_rs"
crate-type = ["cdylib"]

