name: Pre-Commit

on:
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  call-workflow:
    uses: zurich-uas-racing/HomeBase/.github/workflows/pre-commit.yml@main
