[tool.poetry]
name = "vnpt-sparkmagic"
version = "0.1.0"
description = "A collection of utility functions for Jupyter."
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
license = "MIT"

packages = [
    { include = "sparkmagic" }
]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
