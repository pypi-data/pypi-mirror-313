[tool.poetry]
name = "sepstring"
version = "0.1.1"
description = "Simple CLI to split strings"
authors = ["Evan Voyles <ejovo13@yahoo.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
typer = "^0.15.0"

[tool.poetry.scripts]
sep = "sepstring.main:main"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
