[project]
name = "demographic-data-analytic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "lunelson-deloitte", email = "lunelson@deloitte.com" }
]
requires-python = ">=3.10"
dependencies = [
    "attrs>=24.2.0",
    "loguru>=0.7.2",
    "pyspark==3.5.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest>=8.3.4",
]
