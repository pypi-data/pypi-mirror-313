[tool.poetry]
name = "poetrytrial"
version = "0.0.6"
description = "test"
authors = ["Emmanuel Colas"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
trial = "testforbob:main"

[tool.poetry.dependencies]
python = "^3.10"
