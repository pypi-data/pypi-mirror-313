[tool.poetry]
name = "poetrytrial"
version = "0.0.9"
description = "test"
authors = ["Emmanuel Colas"]

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.scripts]
trial = "poetrytrial:main"

[tool.poetry.dependencies]
python = "^3.10"
